function operations(x, y) {
    return console.log(`
    soma = ${x + y}
    subtração = ${x - y}
    multiplicação = ${x * y}
    divisão = ${x / y}`)
}

operations(2, 3)
operations(4,3)
operations(-1, 2)